# Chat-Bot
This project is a web-based chatbot application. The front end is built with HTML for the structure, CSS for styling, and JavaScript for dynamic functionality. When a user sends a message, the application sends a request to the Google Generative AI API (Gemini 1.5 Flash model) to generate a response. 
